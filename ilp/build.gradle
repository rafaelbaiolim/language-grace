apply plugin: 'antlr'
repositories {
    mavenLocal()
    mavenCentral()
    jcenter()
}

dependencies {
    antlr "org.antlr:antlr4:4.5.3"
    compile "org.antlr:antlr4-runtime:4.5.3"
}


task copyTokenFile {
    copy {
        from "src/uem/antlr/IlpLexer.tokens"
        into "src/main/antlr/"
    }
}

generateGrammarSource {
    maxHeapSize = "64m"
    arguments += ['-package', 'uem.antlr']
    outputDirectory = new File("src/uem/antlr/".toString())
}

